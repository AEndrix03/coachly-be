version: "3.9"

services:
  coachly-be:
    container_name: coachly-be

    build:
      context: .
      dockerfile: Dockerfile

    image: coachly/coachly-be:latest
    restart: unless-stopped

    ports:
      - "${COACHLY_PORT:-8443}:8443"

    environment:
      # Spring
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SERVER_PORT: 8443

      # JVM con directory temporanea custom
      JAVA_TOOL_OPTIONS: >
        -XX:+UseContainerSupport
        -XX:MaxRAMPercentage=75
        -Djava.security.egd=file:/dev/./urandom
        -Djava.io.tmpdir=/app/javatmp

      TZ: ${TZ:-Europe/Rome}

    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true

    # /tmp con noexec (sicuro ma limitato)
    # /app/javatmp per Java (eseguibile ma isolato)
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=32m
      - /app/javatmp:rw,nosuid,size=96m

    # Limiti risorse
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: 768M
        reservations:
          cpus: "0.25"
          memory: 256M

    # Logging
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"